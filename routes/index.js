const handleGenres = require("./genres");
const handleHome = require("./home");
const handleMovies = require("./movies");
const handleUsers = require("./users");

module.exports = {
  handleGenres,
  handleHome,
  handleMovies,
  handleUsers,
};
